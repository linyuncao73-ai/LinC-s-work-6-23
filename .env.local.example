# ─── Frontend env (复制为 .env.local) ──────────────────────────────────────────
# 这两个是 Supabase 的「公开」值，受数据库 RLS 策略保护，可以进前端 bundle。
VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR-ANON-KEY

# ⚠️ 不要在这里放 GEMINI_API_KEY。
# Gemini key 只作为 Edge Function 的服务端 secret 存在：
#   supabase secrets set GEMINI_API_KEY=xxxxx
# 见 DEPLOYMENT.md。
